You are fixing a production Python Telegram bot (BDGW VaultFlow) that is crash-looping on Railway. 
Read every file mentioned before touching anything. Make surgical fixes only — no rewrites, no 
architecture changes, no new dependencies. Preserve all existing comments and docstrings.

---

## VERIFIED CRASH (fix this first, it is blocking all other fixes)

FILE: main_bot.py — line 35

PROBLEM:
  logger.info("boot_stage", stage="idle_entered")
  get_logger() returns stdlib logging.Logger. Stdlib loggers reject unknown kwargs.
  TypeError: Logger._log() got an unexpected keyword argument 'stage'
  This crashes on every boot before Pyrogram even starts.

FIX: Replace that single line with:
  logger.info("VaultFlow idle — waiting for shutdown signal.")

---

## CRITICAL: lifecycle.py — entire Telegram startup is dead code

FILE: app/core/lifecycle.py — AppLifecycle.start()

PROBLEM:
  Read the file. The channel seeding block is:
    try:
        channel_service = ChannelService()
        await channel_service.seed_channels()
    except Exception:
        logger.exception("BOOT FAILURE: Channel seeding failed")
        sys.exit(1)

        # 3. Telegram Client   ← THIS IS INSIDE THE EXCEPT BLOCK
        try:
            await self._bot.start()
            ...

  Everything from "# 3. Telegram Client" through the subscription worker start
  is indented ONE LEVEL TOO DEEP — it lives inside the except block, after sys.exit(1).
  sys.exit(1) raises SystemExit making all of it unreachable. The bot connects to 
  MongoDB, seeds channels, logs "VaultFlow fully started", then immediately crashes 
  on line 35. Pyrogram NEVER starts. No handlers are ever registered.

FIX:
  Dedent the entire block from "# 3. Telegram Client" through 
  "await self._subscription_worker.start(bot=self._bot)" (and its try/except wrapper)
  so it is at the SAME indentation level as the channel seeding try/except block above it.
  The referral scheduler block (# 6) is already at the correct level — do not touch it.

  After fixing indentation, also fix these stdlib logger calls in the now-reachable block
  (they use structlog-style kwargs that will crash the same way as line 35):

  WRONG:
    logger.warning("channel_access_check_failed", error=str(verify_err), note="...")
    logger.info("boot_stage", stage="pyrogram_connected", bot_username=me.username, total_handlers=total_handlers)
    logger.exception("Failed to start Pyrogram client", error_type=type(e).__name__, error_message=str(e))
    logger.exception("Failed to start Distribution Engine", error_type=type(e).__name__, error_message=str(e))

  CORRECT replacements:
    logger.warning("Channel access check failed — continuing in degraded state", extra={"ctx_error": str(verify_err)})
    logger.info("Pyrogram connected", extra={"ctx_bot_username": me.username, "ctx_total_handlers": total_handlers})
    logger.exception("Failed to start Pyrogram client", extra={"ctx_error_type": type(e).__name__, "ctx_error": str(e)})
    logger.exception("Failed to start Distribution Engine", extra={"ctx_error_type": type(e).__name__, "ctx_error": str(e)})

---

## CRITICAL: lifecycle.py — subscription worker never instantiated

FILE: app/core/lifecycle.py

PROBLEM:
  self._subscription_worker is set to None in __init__ and never assigned a real instance.
  The call to self._subscription_worker.start(bot=self._bot) raises AttributeError.

FIX:
  In start(), before the try block that calls self._subscription_worker.start(), add:
    self._subscription_worker = SubscriptionWorker()
  
  SubscriptionWorker is imported at the top of the file already. Just add the instantiation.

---

## CRITICAL: scheduler.py — missing imports, missing setup_jobs(), stale lock

FILE: app/scheduler/scheduler.py

PROBLEM 1 — Missing imports:
  Read the file. These are used but not imported:
    asyncio                          (asyncio.create_task, asyncio.sleep used throughout)
    from pymongo.errors import DuplicateKeyError   (used in _acquire_lock)
    WatermarkState                   (used in _run_startup_integrity_scan)
    JobStatus                        (used in _run_startup_integrity_scan — verify it's imported)

FIX: Add the missing imports at the top of the file.
  WatermarkState is defined in app/core/models.py — verify by reading that file before adding the import.

PROBLEM 2 — setup_jobs() is called but does not exist:
  start() calls self.setup_jobs() but no such method is defined.
  The scheduler jobs (_distribution_cycle, _stale_lock_sweep, _deadline_sweep, _collect_metrics)
  are defined as methods but never registered with self._scheduler.

FIX: Replace self.setup_jobs() in start() with inline APScheduler job registration:

    self._scheduler.add_job(
        self._distribution_cycle,
        trigger=IntervalTrigger(seconds=settings.SCHEDULER_INTERVAL_SECONDS),
        id="distribution_cycle",
        replace_existing=True,
        max_instances=1,
        coalesce=True,
    )
    self._scheduler.add_job(
        self._stale_lock_sweep,
        trigger=IntervalTrigger(seconds=120),
        id="stale_lock_sweep",
        replace_existing=True,
        max_instances=1,
        coalesce=True,
    )
    self._scheduler.add_job(
        self._deadline_sweep,
        trigger=IntervalTrigger(seconds=300),
        id="deadline_sweep",
        replace_existing=True,
        max_instances=1,
        coalesce=True,
    )
    self._scheduler.add_job(
        self._collect_metrics,
        trigger=IntervalTrigger(seconds=60),
        id="collect_metrics",
        replace_existing=True,
        max_instances=1,
        coalesce=True,
    )

PROBLEM 3 — Stale lock on restart, scheduler never starts after crash:
  _acquire_lock() uses insert_one() which fails with DuplicateKeyError on every restart
  because the previous lock document is never deleted. The except DuplicateKeyError 
  branch just returns False, so the scheduler never starts after any crash or redeploy.

FIX: At the START of _acquire_lock(), before the try block, add a stale lock cleanup:
    await self._locks.delete_many({"lock_key": "scheduler_active_singleton"})
  
  This is safe — delete_many is idempotent, deletes 0 or more docs. Then the insert_one 
  below it will always succeed on first attempt.

PROBLEM 4 — _distribution_cycle calls get_channel_pending_count() which does not exist:
  self._queue_repo.get_channel_pending_count(source_id) is called but QueueRepository
  has no such method.

FIX: Replace that call inline in _distribution_cycle with a direct count query:
    pending = await self._db[settings.QUEUE_COLLECTION].count_documents({
        "source_channel_id": source_id,
        "status": {"$in": ["pending", "watermarking", "ready", "locked", "delivering"]}
    })

PROBLEM 5 — _deadline_sweep calls get_deadline_exceeded_jobs() which does not exist:
  QueueRepository has no get_deadline_exceeded_jobs() method.

FIX: Replace the body of _deadline_sweep() with a direct DB query:
    async def _deadline_sweep(self) -> None:
        try:
            now = datetime.now(timezone.utc)
            queue = self._db[settings.QUEUE_COLLECTION]
            cursor = queue.find({
                "status": {"$in": ["pending", "locked", "processing", "watermarking"]},
                "queue_deadline": {"$lt": now, "$ne": None},
            })
            async for job in cursor:
                await self._queue_repo.move_to_dead_letter(str(job["_id"]), "deadline_exceeded")
        except Exception as e:
            logger.error(f"Deadline sweep FAILED: {e}")

---

## BROKEN: dispatcher.py — missing import asyncio

FILE: app/distribution/dispatcher.py

PROBLEM:
  asyncio.Event() and asyncio.create_task() are used for the heartbeat mechanism
  but import asyncio is missing from the file.

FIX: Add import asyncio at the top of the file with the other stdlib imports.

---

## BROKEN: submission_handler.py and support_handler.py — _redis NameError

FILE: app/handlers/submission_handler.py

PROBLEM:
  In handle_submit_menu():
    redis = get_redis()
    ...
    await _redis.set(spam_key, "1", ex=1)   ← _redis is not defined, should be redis

FIX: Change _redis.set to redis.set in that line.

FILE: app/handlers/support_handler.py

PROBLEM:
  In handle_support_menu():
    redis = get_redis()
    ...
    await _redis.set(spam_key, "1", ex=1)   ← same bug

FIX: Change _redis.set to redis.set in that line.

---

## BROKEN: watermark/worker_pool.py — missing imports and missing method

FILE: app/watermark/worker_pool.py

PROBLEM 1 — Missing imports:
  _process_group uses:
    List[dict]              → needs: from typing import List (or use list[dict] since Python 3.12)
    MediaFileNotFoundError  → needs: from app.core.exceptions import MediaFileNotFoundError
    DispatcherError         → needs: from app.core.exceptions import DispatcherError
    FloodWait               → needs: from pyrogram.errors import FloodWait

FIX: Add all four to the imports at the top of the file.
  Since this is Python 3.12, you can also just change List[dict] to list[dict] inline 
  in the method signature instead of adding the typing import — either is acceptable.

PROBLEM 2 — _resolve_media_path() called but not defined:
  self._resolve_media_path(job, job_id) is called in _process_group but WatermarkWorker
  has no such method. download_with_refresh is imported but never wired up.

FIX: Add this method to the WatermarkWorker class:

    async def _resolve_media_path(self, job: dict, job_id: str) -> Optional[str]:
        bot = get_bot()
        return await download_with_refresh(
            client=bot,
            job_doc=job,
            dest_dir=settings.PROCESSED_MEDIA_DIR,
            job_id=job_id,
        )

---

## BROKEN: queue_repository.py — missing methods and duplicate return

FILE: app/repositories/queue_repository.py

PROBLEM 1 — Duplicate unreachable return:
  claim_next() has two return claimed statements — the second one at the end of the 
  method is unreachable dead code. Remove it.

PROBLEM 2 — get_recently_posted_content_ids() missing:
  FairnessSelector calls this method but it does not exist on QueueRepository.

FIX: Add this method to QueueRepository:

    async def get_recently_posted_content_ids(
        self, source_channel_id: str, hours: int = 168
    ) -> set:
        cutoff = datetime.now(timezone.utc) - timedelta(hours=hours)
        cursor = self._queue.find(
            {
                "source_channel_id": source_channel_id,
                "status": JobStatus.COMPLETED,
                "completed_at": {"$gte": cutoff},
            },
            {"content_id": 1},
        )
        ids = set()
        async for doc in cursor:
            if doc.get("content_id"):
                ids.add(doc["content_id"])
        return ids

---

## SECONDARY: referral system init failure

The logs show "Failed to initialize Referral System" on every boot. 

FILE: app/core/lifecycle.py (or wherever referral init is called — read the file to find it)

Read app/referral/db.py and app/referral/scheduler.py. The ReferralRepository.__init__() 
calls DatabaseManager.get_db() at construction time. If called before DatabaseManager.connect() 
completes, it raises RuntimeError: "Database not initialized."

Check where in lifecycle.py or the referral scheduler the ReferralRepository is 
instantiated. If it's being instantiated at module import time or too early in the boot 
sequence, wrap it in a try/except that logs a warning but does not crash the boot, OR 
delay construction until after the DB connection is confirmed.

The "Failed to initialize Referral System" should be a warning, not a crash trigger.
Ensure the except block in lifecycle.py around the referral scheduler logs at WARNING 
level and continues — never sys.exit() for the referral system.

---

## SECONDARY: Index verification DEGRADED on every boot

The logs show "DEGRADED: Index verification failed" consistently. This is non-fatal per 
the existing code but it fires on every boot which means indexes are being recreated 
with conflicting options every time.

FILE: app/core/database.py — _safe_create() and _ensure_indexes()

Read the file. The vault collection has _safe_create called TWICE — once in the main 
index block and once for the M3 checksum/cooldown indexes. Both calls create indexes on 
the same collection. If any index name already exists with different options, the second 
call triggers the full drop_indexes() + recreate path, which is destructive and slow.

FIX: Merge all vault indexes into a single _safe_create(settings.VAULT_COLLECTION, [...]) 
call. Find both calls to _safe_create(settings.VAULT_COLLECTION, ...) in _ensure_indexes() 
and combine their index lists into one call.

---

## EXECUTION ORDER

1. Read main_bot.py
2. Fix main_bot.py line 35

3. Read app/core/lifecycle.py in full
4. Fix the indentation (dead code block)
5. Fix all stdlib logger calls with structlog-style kwargs in lifecycle.py
6. Add self._subscription_worker = SubscriptionWorker() instantiation

7. Read app/scheduler/scheduler.py in full
8. Read app/core/models.py to confirm WatermarkState import path
9. Add all missing imports to scheduler.py
10. Replace self.setup_jobs() with inline add_job() calls
11. Fix _acquire_lock() stale lock cleanup
12. Fix _distribution_cycle pending count query
13. Fix _deadline_sweep to use direct DB query

14. Read app/distribution/dispatcher.py
15. Add import asyncio

16. Read app/handlers/submission_handler.py
17. Fix _redis → redis

18. Read app/handlers/support_handler.py  
19. Fix _redis → redis

20. Read app/watermark/worker_pool.py in full
21. Add missing imports
22. Add _resolve_media_path() method

23. Read app/repositories/queue_repository.py in full
24. Remove duplicate return claimed
25. Add get_recently_posted_content_ids() method

26. Read app/core/database.py
27. Merge the two vault _safe_create() calls into one

28. Read app/core/lifecycle.py again and app/referral/scheduler.py
29. Fix referral init failure (wrap in non-fatal try/except if not already)

After all fixes, output a summary listing every file changed and every line modified.
Do not modify any file not listed above. Do not change method signatures unless 
explicitly required by a fix above. Do not add new dependencies.