
You are a senior backend architect and Telegram infrastructure engineer.

You are working on a production Telegram automation system called BDGW VaultFlow.

Your task is to FULLY REPAIR the queue architecture, vault linkage system, album delivery system, retry logic, and production deployment safety.

The repository already exists.

CRITICAL:
Do NOT produce partial fixes.
Do NOT create mock code.
Do NOT leave TODOs.
Do NOT simplify architecture.
Do NOT break existing moderation/admin flows.

You must make the system production-ready and deployable.

━━━━━━━━━━━━━━━━━━━━━━
PRIMARY ROOT CAUSE
━━━━━━━━━━━━━━━━━━━━━━

Current failures:

- queue jobs missing vault references
- albums fragment during scheduling/retries
- stale Telegram file_id fallback corrupts delivery
- retries loop forever on permanent failures
- vault linkage not persisted
- restart recovery corrupts jobs
- raw file_id fallback causes stale media failures

Observed errors:

- cannot link to vault
- missing source_chat_id/source_message_id
- album delivery failures
- dead-letter floods
- reconnect storms

Architecture currently mixes:
- source identity
- vault identity
- queue identity

This must be unified.

━━━━━━━━━━━━━━━━━━━━━━
REQUIRED ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━━━

QUEUE JOBS MUST ALWAYS CONTAIN:

- content_id
- source_channel_id
- source_message_id
- vault_chat_id
- vault_message_id
- media_group_id
- target_channel_ids
- media_type

Vault references are mandatory.

NO FALLBACKS.

━━━━━━━━━━━━━━━━━━━━━━
REQUIRED DELIVERY FLOW
━━━━━━━━━━━━━━━━━━━━━━

ALL delivery MUST use:

copy_message()
copy_media_group()

FROM vault channel ONLY.

Never reconstruct albums using file_id.
Never send InputMedia from stale file_ids.
Never fetch from original source channel.

Telegram CDN references must come ONLY from vault storage.

Use:

await bot.copy_message(...)
await bot.copy_media_group(...)

Reference:
https://docs.pyrogram.org/api/methods/copy_media_group

━━━━━━━━━━━━━━━━━━━━━━
REQUIRED FIXES
━━━━━━━━━━━━━━━━━━━━━━

1. Replace QueueJob schema.

Add:

- vault_chat_id
- vault_message_id
- source_message_id
- media_group_id
- retry_count
- delivered_targets
- failed_targets
- lock state

2. Create strict queue validation.

Reject malformed jobs BEFORE insertion.

3. Remove ALL raw file_id fallbacks.

Delete any logic like:
- "trying raw file_id fallback"
- "using raw file_id (may be stale)"

4. Add permanent vs retryable exception hierarchy.

Retryable:
- FloodWait
- Timeout
- RPC transient failures
- Rate limits

Permanent:
- missing vault refs
- deleted vault message
- malformed albums
- invalid jobs

5. Rewrite delivery pipeline.

Album delivery MUST use:

await bot.copy_media_group(
    chat_id=target,
    from_chat_id=vault_chat_id,
    message_id=first_album_message_id
)

Single delivery MUST use:

await bot.copy_message(...)

6. Fix moderation ingestion.

After content is copied into vault:
store:

- vault_chat_id
- vault_message_id

inside MongoDB vault documents.

7. Fix queue repository.

Never rebuild partial jobs.
Never use QueueJob(...).model_dump() if it strips fields.

Preserve original Mongo documents fully.

8. Fix album scheduling.

Albums must:
- enqueue together
- retry together
- deliver together
- fail together

Never split albums into independent jobs.

9. Add production-safe indexes.

Required indexes:

vault_chat_id + vault_message_id unique
media_group_id index

10. Replace weak content_id generation.

DO NOT use:
f"{source_channel_id}_{msg.id}"

Use sha256 hash with:
source_channel_id
message_id
file_unique_id

11. Add structured logging everywhere.

Every log MUST include:
- job_id
- target_id
- vault_message_id
- media_group_id
- retry_count
- failure_type

12. Add restart safety.

Workers must recover:
- stale locks
- floodwait state
- partial jobs

13. Ensure no duplicate album delivery.

14. Ensure queue persistence survives restart safely.

15. Make deployment production-ready.

━━━━━━━━━━━━━━━━━━━━━━
DEPLOYMENT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━

Generate:

- Dockerfile
- docker-compose.yml
- healthcheck support
- restart-safe worker startup
- Railway-ready deployment
- environment validation
- graceful shutdown
- Mongo reconnect safety
- Redis reconnect safety

━━━━━━━━━━━━━━━━━━━━━━
TECH STACK
━━━━━━━━━━━━━━━━━━━━━━

Python 3.12
Pyrogram
MongoDB
Redis
AsyncIO
Motor
APScheduler

━━━━━━━━━━━━━━━━━━━━━━
CRITICAL ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━━━

Think like a production SRE and backend architect.

You must:
- detect hidden race conditions
- preserve album ordering
- prevent duplicate sends
- prevent retry storms
- prevent restart corruption
- prevent queue corruption
- prevent stale file_id usage
- prevent dead-letter amplification
- preserve Telegram floodwait safety

━━━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━━━

Generate COMPLETE production-ready code changes.

Modify files directly.

Do NOT give explanations only.

Generate:
- full file replacements
- exact patches
- migration-safe changes
- deployment-ready configs

After modifications:
- audit for race conditions
- audit for async safety
- audit for Mongo consistency
- audit for Telegram edge cases
- audit for restart safety

Then generate:

1. migration steps
2. Mongo cleanup commands
3. deployment steps
4. rollback strategy
5. verification checklist

━━━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━━━

Do NOT preserve broken architecture for compatibility.

Correctness and production reliability are more important than backward compatibility.

Broken queue jobs may be purged if necessary.

Focus on:
- immutable vault references
- reliable album delivery
- restart safety
- queue consistency
- Telegram-native media copying
- production-grade observability