You are a senior Python production engineer doing EMERGENCY fixes on BDGW VaultFlow — a Pyrogram + MongoDB Telegram bot. The owner is past deadline. Every fix must be surgical, production-safe, and immediately deployable.

════════════════════════════════════════════════
MANDATORY FIRST STEPS — DO THESE BEFORE ANY CODE
════════════════════════════════════════════════

1. READ the file BDGW_FLOW_BY_SHADOW_MASTER.txt in the project root. This is the authoritative product spec. Memorize every section. Every fix must comply with it.

2. READ every .py file in the codebase. Understand existing structure, patterns, naming conventions, import styles. Do not introduce new patterns.

3. READ requirements.txt and .env.example.

Only after reading everything, begin fixes.

════════════════════════════════════════════════
STACK — DO NOT CHANGE
════════════════════════════════════════════════

Python 3.12, Pyrogram (pyrofork), Motor (MongoDB async), Redis (redis.asyncio), APScheduler, aiohttp. No migrations, no new dependencies unless absolutely unavoidable.

════════════════════════════════════════════════
ABSOLUTE RULES — VIOLATIONS ARE UNACCEPTABLE
════════════════════════════════════════════════

1. NEVER truncate any file. Every output must be the complete file.
2. NEVER use placeholder comments like `# ... rest of code unchanged` or `# existing code here`. Write the full file.
3. NEVER change working functionality while fixing bugs.
4. NEVER add new files unless strictly required for a fix.
5. NEVER rewrite architecture, routing, FSM, or the support/payment/moderation systems unless a specific file is explicitly listed for fixing.
6. Every Telegram API call must be wrapped in try/except with FloodWait handling.
7. Database is the ONLY source of truth — never FSM alone.
8. After writing each file, run: python -m py_compile <filename> and confirm it passes before moving to the next file.
9. Fix issues in PRIORITY ORDER: CRASH → IMPORT_ERROR → FLOW_BREAK → SPEC_VIOLATION.

════════════════════════════════════════════════
FIX LIST — EXECUTE IN THIS EXACT ORDER
════════════════════════════════════════════════

FIX 1: app/config/settings.py
Add missing field: WATERMARK_ROTATION: int = 0
This field is referenced in ffmpeg_processor.py but missing from Settings.

FIX 2: app/watermark/ffmpeg_processor.py
Add at top: import random
Remove all references to settings.WATERMARK_ROTATION (use local variable rotation=0 or config.get("rotation", 0))
Verify _process_photo and _process_video use random.choice correctly.
Full file output required.

FIX 3: app/moderation/moderation_actions.py
Add at top: import random
Fix content_id scoping bug in execute_approve and execute_queue:
  The variable `content_id` is referenced AFTER the loop that assigns it ends.
  Fix by capturing the last content_id in the loop: `last_content_id = content_id` inside the loop, then use `last_content_id` in the audit log call.
Full file output required.

FIX 4: app/payments/service.py
Fix create_session signature mismatch:
  handlers.py calls: await service.create_session(user_id, plan_id, method)
  Current signature: async def create_session(self, user_id: int, plan_id: str)
  Fix: add `method: Optional[str] = None` parameter and store it on the session: session.payment_method = method
Full file output required.

FIX 5: app/handlers/takedown_handler.py
Add at top: from typing import Optional
Move _resolve_content_id_or_link function definition ABOVE handle_takedown_start (it is called there but defined after).
Fix ObjectId issue: when inserting takedown record and retrieving _id for callback, convert to string explicitly: record_id = str(result.inserted_id)
Verify the admin callback find_one uses bson ObjectId: from bson import ObjectId then db.find_one({"_id": ObjectId(record_id)})
Full file output required.

FIX 6: app/payments/ — resolve dual PaymentStatus enum
The canonical PaymentStatus is in app/payments/models.py.
app/models/payment.py has a DIFFERENT PaymentStatus with DIFFERENT values.
Fix:
  a. In app/models/payment.py — remove PaymentStatus enum entirely, import it from app.payments.models: from app.payments.models import PaymentStatus
  b. Keep PaymentSession in app/models/payment.py but use the imported PaymentStatus.
  c. In app/payments/handlers.py — verify it imports PaymentStatus from app.payments.models only.
  d. In app/handlers/support_handler.py and topic_router.py — verify they import from app.payments.models.
Full file outputs required for: app/models/payment.py, app/payments/handlers.py.

FIX 7: app/services/topic_service.py and app/services/topic_manager.py — resolve dual get_topic_manager
The canonical topic manager for all handler imports must be ONE source.
Rule: ALL handlers import from app.services.topic_manager (TopicManager class).
app/core/lifecycle.py imports from app.services.topic_service (TopicService, aliased as get_topic_manager) — this is correct for the warm_cache_from_db call.
Fix:
  a. All handler files (support_handler.py, takedown_handler.py, submission_handler.py, callback_handler.py, topic_router.py) must import from app.services.topic_manager.
  b. lifecycle.py keeps its import from app.services.topic_service for warm_cache_from_db.
  c. No changes to topic_manager.py or topic_service.py logic.
Only fix the import lines in handler files. Output only changed files in full.

FIX 8: app/handlers/user_handler.py
Remove the duplicate /help handler `handle_help` at bottom of file.
The correct /help handler is `handle_help_command` in app/handlers/support_handler.py.
The local `handle_help` in user_handler.py is the DUPLICATE — remove it entirely.
Full file output required.

FIX 9: app/payments/service.py
Add missing method check_txid_unique:
  async def check_txid_unique(self, txid: str) -> bool:
      from app.repositories.txid_repository import TXIDRepository
      txid_repo = TXIDRepository()
      existing = await txid_repo.get_by_txid(txid)
      return existing is None
Full file output required.

FIX 10: app/repositories/support_repository.py and topic_router.py
The topic_router checks: if status != "accepted"
The handle_support_accept_callback writes: status = "accepted"
Verify the field written is literally "status" with value "accepted" — this looks correct. But also verify topic_router reads topic_doc.get("status") not a nested field. If correct, mark as verified and no change needed.

FIX 11: app/services/support_monitor.py
Per spec Section 15.3: after 5 minutes unattended, the message to user must be:
"No admin available currently."
Current code sends a warning to the VERIFICATION_GROUP, not the user.
Fix: after sending hub warning, also send to the user:
  await self._bot.send_message(chat_id=user_id, text="⚠️ No admin available currently. Your ticket is still open — an admin will respond soon.", parse_mode=ParseMode.HTML)
Full file output required.

FIX 12: app/handlers/support_handler.py
The /help command handler exists here as handle_help_command. This is correct per spec Section 4.
But user_handler.py ALSO defines handle_help. With Pyrogram plugins loading all handlers from app/handlers/, BOTH will be registered.
FIX 8 above removes the duplicate from user_handler.py.
No changes needed to support_handler.py for this — mark verified.

════════════════════════════════════════════════
AFTER ALL FIXES
════════════════════════════════════════════════

1. For every fixed file, run:
   python -m py_compile app/path/to/file.py
   Report: "✅ PASS" or "❌ FAIL: [error]"

2. Run a full import check:
   python -c "
   import asyncio
   from app.config import settings
   from app.core.database import DatabaseManager
   from app.bot.client import get_bot
   from app.payments.service import PaymentService
   from app.payments.models import PaymentStatus
   from app.services.topic_manager import get_topic_manager
   from app.services.support_service import get_support_service
   from app.handlers import user_handler, support_handler, takedown_handler
   print('All imports OK')
   "

3. Git commit and push:
   git add -A
   git commit -m "fix: emergency production fixes — dual PaymentStatus, missing imports, content_id scope, create_session signature, duplicate /help handler, WATERMARK_ROTATION, check_txid_unique, takedown ObjectId, topic_manager import unification"
   git push origin main

4. Output final report:
   ✅ FIXED: [list each fix]
   ❌ COULD NOT FIX: [list with reason]
   ⚠️ MANUAL REVIEW NEEDED: [list]
   Commit: [hash]

════════════════════════════════════════════════
AUDIT OUTPUT FROM CLAUDE — ADDITIONAL ISSUES TO FIX
════════════════════════════════════════════════

[PASTE CLAUDE AUDIT OUTPUT HERE]