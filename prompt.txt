
# BDGW VAULTFLOW — PRODUCTION DEPLOY FIX
# MASTER PROMPT — READ EVERY WORD BEFORE WRITING A SINGLE LINE

You are a senior Python backend engineer doing a surgical production fix on BDGW VaultFlow.
This bot is LIVE. Users are testing it RIGHT NOW. You have one job: fix the four confirmed
crashes listed below and nothing else. Do not refactor. Do not improve. Do not touch anything
that is not directly causing a crash. Every file you touch must be output in full, complete,
copy-paste ready form with no truncation and no placeholder comments.

---

## ABSOLUTE RULES — VIOLATING ANY OF THESE ENDS THE TASK

1. NEVER use logger.info("msg", key=value) — stdlib Logger crashes on bare kwargs.
   ONLY valid: logger.info("msg") or logger.info("msg", extra={"key": value})

2. NEVER output a file with "..." truncation. Every file must be 100% complete.

3. NEVER touch files not listed in SCOPE below.

4. NEVER move business logic. NEVER change architecture. NEVER refactor.

5. READ the PaymentStatus definition before touching any file that uses it.
   Do not assume attribute names. Read first. Fix second.

6. READ app/payments/handlers.py before fixing app/handlers/payment_handler.py.
   The import must match the actual exported function name exactly.

7. If you cannot determine the correct fix from the files provided, say:
   "I need the content of <filepath> to complete this fix."
   Do not guess. Do not invent attribute names or function names.

8. Do not add any new dependencies, imports, collections, or services.

9. Do not add new exception types or custom exception classes.

10. Output format is strict — see OUTPUT FORMAT section below. No deviations.

---

## CONFIRMED CRASHES FROM PRODUCTION LOGS

### CRASH 1 — ImportError blocks Pyrogram startup entirely
File "/app/app/handlers/payment_handler.py", line 10
ImportError: cannot import name 'handle_payment_inputs' from 'app.payments.handlers'
Did you mean: 'handle_payment_status'?
The Pyrogram plugin loader imports every file in app/handlers/ at startup.
This import error kills the entire bot before a single handler registers.

Fix: Open app/payments/handlers.py. Find the actual exported function names at
the module level (decorated with @Client.on_callback_query or @Client.on_message).
Open app/handlers/payment_handler.py. Change the import list to match exactly
what is exported. Nothing else.

### CRASH 2 — PaymentStatus attribute error kills payment callbacks
Unhandled exception: type object 'PaymentStatus' has no attribute 'WAITING_TXID'
Something references PaymentStatus.WAITING_TXID but that attribute does not exist
on the enum. It appears in app/payments/repository.py in the get_active_session method.

Fix: Open app/payments/models.py. Read the PaymentStatus enum definition.
Find the correct attribute name for the "waiting for TXID" state.
Open app/payments/repository.py. Find every reference to PaymentStatus.WAITING_TXID.
Replace with the correct attribute name. Nothing else.

### CRASH 3 — CallbackQuery.chat AttributeError kills moderation callbacks
'CallbackQuery' object has no attribute 'chat'
In Pyrogram, CallbackQuery has no .chat attribute. The correct path is
callback.message.chat (when the callback originated from an inline keyboard on a message).

Fix: Search app/handlers/ for any handler function that receives a CallbackQuery
argument and accesses .chat directly on it (i.e. callback.chat or query.chat).
Change every instance to callback.message.chat or callback.message.chat.id as appropriate.
Guard with: if not callback.message or not callback.message.chat: return
Do not touch handlers that already correctly use callback.message.chat.

### CRASH 4 — Forum topic creation crashes support flow
forum_topic_creation_unexpected_error (repeated 3 times, then forum_topic_creation_failed)
HANDLER: handle_support_menu entered
The topic creation is retrying 3 times then failing silently. The user sees no error.

Fix: Open app/services/topic_service.py. Find _create_named_topic.
The method calls client.invoke(raw.functions.channels.CreateForumTopic(...)).
This requires the bot to be admin with manage_topics permission in the group.
The unexpected_error suggests a non-FloodWait, non-Forbidden exception is being
raised — likely a Pyrogram version incompatibility with raw API.

Add a fallback: if raw API fails with any Exception that is not FloodWait or Forbidden,
catch it, log it with full exc_info=True, then try the high-level API instead:
  result = await client.create_forum_topic(
      chat_id=settings.VERIFICATION_GROUP_ID,
      title=title
  )
  return result.id

If that also fails, raise the exception so the caller can send the user a fallback message.
In app/handlers/support_handler.py in handle_support_menu, wrap the topic creation call
in try/except and if it fails, edit the message to say:
"Support is temporarily unavailable. Please try again in a few minutes."
Do not crash. Do not leave the user with a broken state.

---

## SCOPE — ONLY THESE FILES MAY BE MODIFIED

1. app/handlers/payment_handler.py     — fix CRASH 1 (wrong import name)
2. app/payments/repository.py          — fix CRASH 2 (WAITING_TXID attribute)
3. app/handlers/support_handler.py     — fix CRASH 3 (callback.chat) + CRASH 4 (fallback)
4. app/services/topic_service.py       — fix CRASH 4 (raw API fallback)

You may read any file to understand context. You may only write to the four files above.

If CRASH 2 requires a change to app/payments/models.py (e.g. the attribute is genuinely
missing from the enum), you may add that file to scope and output it as well. State clearly
that you are adding it and why.

If CRASH 3 also appears in app/payments/handlers.py or app/handlers/callback_handler.py,
add those files to scope and output them. State clearly that you are adding them and why.

---

## OUTPUT FORMAT — STRICTLY ENFORCED

For each file you modify, output this exact structure:

---
### FILE: <exact filepath from repo root>
**Root cause:** <one sentence, specific, no fluff>
**Lines changed:** <list each change as "line N: old → new — reason">

```python
<COMPLETE FILE CONTENT — ZERO TRUNCATION — ZERO PLACEHOLDERS>
```
---

After all files, output:

---
### DEPLOY CHECKLIST
- [ ] CRASH 1 fixed: ImportError resolved, Pyrogram loads all plugins
- [ ] CRASH 2 fixed: PaymentStatus.WAITING_TXID replaced with correct attribute
- [ ] CRASH 3 fixed: all callback.chat → callback.message.chat with guard
- [ ] CRASH 4 fixed: topic creation has raw API fallback + user-facing error on failure
- [ ] Zero new imports added that are not already in requirements.txt
- [ ] Zero logger calls with bare kwargs in any modified file
- [ ] All modified files are 100% complete with no truncation
---

Do not output any other text. No preamble. No summary after the checklist.
Start immediately with the first ### FILE: block.

How to use this:
After pasting the prompt, immediately append the raw content of these files in this order, each prefixed with its path:
=== app/payments/models.py ===
<paste full file>

=== app/payments/handlers.py ===
<paste full file>

=== app/payments/repository.py ===
<paste full file>

=== app/handlers/payment_handler.py ===
<paste full file>

=== app/handlers/support_handler.py ===
<paste full file>

=== app/handlers/callback_handler.py ===
<paste full file>

=== app/services/topic_service.py ===
<paste full file>