You have completed an 8-phase implementation. Now perform a FULL pre-deploy audit.
Do not skip any step. Do not report done until every check passes.

## STEP 1 — Syntax audit of ALL new and modified files

Run:
  python preflight.py

Must show: "All checks passed — safe to deploy."
If any file fails: fix it before continuing.

Then run py_compile on every new payments module:
  python -m py_compile app\payments\models.py; Write-Host "models OK"
  python -m py_compile app\payments\repository.py; Write-Host "repository OK"
  python -m py_compile app\payments\service.py; Write-Host "service OK"
  python -m py_compile app\payments\handlers.py; Write-Host "handlers OK"

All must print OK.

## STEP 2 — Import chain audit (most critical)

Run this EXACTLY and report every line:
  python -c "
import sys
sys.path.insert(0, '.')
import app.handlers, pkgutil
errors = []
for f, name, _ in pkgutil.walk_packages(app.handlers.__path__, 'app.handlers.'):
    try:
        __import__(name)
        print(f'OK: {name}')
    except Exception as e:
        print(f'FAIL: {name} -> {type(e).__name__}: {e}')
        errors.append(name)
print(f'Total errors: {len(errors)}')
print(f'All handlers importable: {len(errors) == 0}')
"

Expected: Total errors: 0
If ANY line shows FAIL: fix that module immediately. Do not deploy.

Then verify payments module imports cleanly:
  python -c "
import sys
sys.path.insert(0, '.')
modules = [
    'app.payments.models',
    'app.payments.repository',
    'app.payments.service',
    'app.payments.handlers',
]
for m in modules:
    try:
        __import__(m)
        print(f'OK: {m}')
    except Exception as e:
        print(f'FAIL: {m} -> {type(e).__name__}: {e}')
"

All must print OK.

## STEP 3 — Module-level infrastructure call scan

Run:
  Select-String -Path "app\handlers\*.py","app\payments\*.py" `
    -Pattern "^get_redis\(\)|^get_db\(\)|^_redis\s*=\s*get_redis|^_db\s*=\s*get_db" `
    | Select-Object Path, LineNumber, Line | Format-Table -AutoSize

Any match at column 0 or module level is a critical bug.
Every match must be INSIDE a function body.
Report every line found.

## STEP 4 — Verify payments module is registered as Pyrogram plugin

Run:
  Get-Content app\bot\client.py | ForEach-Object -Begin {$i=1} -Process {"$i`t$_"; $i++}

Confirm plugins= dict includes app.payments OR app.payments.handlers.
If payments handlers are NOT in the plugin path: Pyrogram will never load them.

If missing, the plugins dict must be updated to include the payments package:
  plugins=dict(root="app.handlers")  # handlers only — BAD if payments is separate
  plugins=dict(root="app")           # too broad — BAD

Correct approach: payments handlers must either be:
  A) Inside app\handlers\payment_handler.py (already in plugin path), OR
  B) The plugins root must be broadened to include app\payments\

Report what currently exists and whether handlers will be loaded.

## STEP 5 — Verify CallbackQuery bug fix

Run:
  Select-String -Path "app\handlers\*.py","app\payments\*.py" `
    -Pattern "query\.chat|callback\.chat" `
    | Select-Object Path, LineNumber, Line | Format-Table -AutoSize

Any match is a bug. Must return zero results.

## STEP 6 — Verify no sys.exit() in handlers or payments

Run:
  Select-String -Path "app\handlers\*.py","app\payments\*.py" `
    -Pattern "sys\.exit" `
    | Select-Object Path, LineNumber, Line | Format-Table -AutoSize

Must return zero results.

## STEP 7 — Verify atomic lock implementation exists

Run:
  Select-String -Path "app\payments\repository.py" `
    -Pattern "acquire_processing_lock|find_one_and_update|processing" `
    | Select-Object LineNumber, Line | Format-Table -AutoSize

Must show the atomic lock function exists.
If missing: implement it before deploying.

## STEP 8 — Verify payment states are defined

Run:
  Select-String -Path "app\payments\models.py" `
    -Pattern "waiting_payment_details|waiting_txid|under_review|approved|rejected|expired" `
    | Select-Object LineNumber, Line | Format-Table -AutoSize

Must show all required states.

## STEP 9 — Verify UX changes

Run:
  Select-String -Path "app\services\onboarding_service.py","app\bot\keyboards.py" `
    -Pattern "premier|automation ecosystem|unlock amazing|upgrade your experience|SaaS" `
    | Select-Object Path, LineNumber, Line | Format-Table -AutoSize

Must return ZERO matches. If any marketing language remains: remove it.

Run:
  Select-String -Path "app\bot\keyboards.py" `
    -Pattern "Premium|Submit|Anonymous|Referral|Status|Support" `
    | Select-Object LineNumber, Line | Format-Table -AutoSize

Must show all 5 menu buttons exist.

## STEP 10 — Verify timeout logic starts AFTER delivery

Read app\payments\service.py or app\payments\timeouts.py.
Find where timeout timer starts.
Confirm it is called AFTER:
  await bot.send_message(...)  # delivery confirmed

NOT before.

Report the exact function and line where timeout starts.
If timeout starts before delivery: fix it.

## STEP 11 — Check for bare except patterns

Run:
  Select-String -Path "app\payments\*.py" `
    -Pattern "except:\s*$|except Exception:\s*$" `
    | Select-Object Path, LineNumber, Line | Format-Table -AutoSize

Any match that has no variable (except Exception as e:) is a silent swallower.
Fix every match to use: except Exception as e: with logger.exception()

## FINAL REPORT FORMAT

Report in this exact order:
1. preflight.py output (must show "All checks passed")
2. Handler import audit result (must show "Total errors: 0")
3. Payments import audit result (all OK)
4. Plugin registration status (are payment handlers reachable by Pyrogram?)
5. CallbackQuery bug check (must be zero matches)
6. sys.exit check (must be zero matches)
7. Atomic lock exists (yes/no + line number)
8. Payment states defined (yes/no + list found)
9. Marketing language removed (yes/no)
10. Timeout placement (after delivery: yes/no + line reference)
11. Bare except count (must be zero)

THEN:
If all 11 checks pass: output exactly "SAFE TO DEPLOY"
If any check fails: list every failure and fix before outputting anything.

Do NOT output "SAFE TO DEPLOY" until all 11 checks pass.