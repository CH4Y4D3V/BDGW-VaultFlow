====================================================================
GEMINI DELIVERY CROSS-CHECK REPORT
BDGW VAULTFLOW — POST-AUDIT VERIFICATION
====================================================================
Comparing Gemini's completion report against MASTER REFERENCE v1.0
====================================================================

VERDICT LEGEND:
  ✅ CONFIRMED — Gemini explicitly reported this as done
  ⚠️  VAGUE     — Gemini mentioned it but gave no proof of correctness
  ❌ MISSING   — Not mentioned at all in Gemini's report
  🔴 CRITICAL  — Missing AND directly affects delivery/user experience

====================================================================
SYSTEM-BY-SYSTEM GAPS
====================================================================

SYSTEM 1 — USER REGISTRATION & ONBOARDING
  ✅ UserRepository created
  ✅ join_date and metadata stored
  ⚠️  "Onboarding shown once" — mentioned but no confirmation that
      the check is DB-based not FSM-based. If FSM: breaks on restart.
  ❌ No mention of what the onboarding message actually says
     (Community purpose, rules, submission rules, reporting system)

ACTION NEEDED:
  Verify: users table has `onboarded` boolean column.
  Verify: /start checks DB row, not FSM state, for first-time check.

--------------------------------------------------------------------

SYSTEM 2 & 3 — MENUS & COMMANDS
  ✅ 4-row main menu layout restored
  ✅ /start, /takedown, /help registered
  ⚠️  "Professional layout" — need to verify button text matches spec:
      💎 Premium Access
      📤 Submit Content Anonymously
      🎁 Referral Program  |  📊 My Status
      🆘 Need Help
  ❌ No confirmation that /main was REMOVED (old command)
  ❌ No confirmation command descriptions are user-friendly text

ACTION NEEDED:
  Verify set_my_commands has exactly 3 entries, no /main leftover.
  Verify descriptions shown in Telegram menu are plain English.

--------------------------------------------------------------------

SYSTEM 4 — DOT-SLASH AUTO-DELETE
  ✅ Implemented, 10-second delay, silent, private chat only
  ⚠️  "Private chats" — spec says bot chat, confirm it does not
      accidentally fire in group chats or the hub

ACTION NEEDED:
  Verify filter is: message.chat.type == "private" AND
  message.text.startswith("./")

--------------------------------------------------------------------

SYSTEM 5 — PREMIUM & PAYMENT
  ❌ NOT MENTIONED in Gemini's report at all
  🔴 CRITICAL — This is the core revenue system

  Not confirmed:
    - Package selection flow
    - Payment method selection
    - Payment request DB record creation
    - Hub topic card creation
    - Admin "Send Payment Details" button
    - Session activates ONLY after delivery confirmation
    - 20-minute session timeout worker
    - TXID proof collection flow
    - Admin ✅ Approve | ❌ Reject buttons
    - Atomic subscription activation (9-step)
    - One-time invite generation (30 min expiry)
    - Invite delivery to user

ACTION NEEDED:
  This system must be manually verified in full before sign-off.
  Ask Gemini or inspect code directly.

--------------------------------------------------------------------

SYSTEM 6 — SUBSCRIPTION LIFECYCLE
  ❌ NOT MENTIONED
  🔴 CRITICAL

  Not confirmed:
    - 7-day / 3-day / expiry-day notification worker
    - Auto-removal from premium groups on expiry
    - Group removal notification sent (this one IS allowed)
    - Subscription status updated to EXPIRED

ACTION NEEDED:
  Check if a scheduler/APScheduler/asyncio task exists for expiry.

--------------------------------------------------------------------

SYSTEM 7 — TXID PROTECTION
  ❌ NOT MENTIONED explicitly
  🔴 CRITICAL — Fraud prevention

  Not confirmed:
    - txid_registry table or unique constraint exists
    - Duplicate TXID auto-rejected before admin sees proof
    - TXID inserted atomically with approval

ACTION NEEDED:
  Check DB schema for txid_registry or unique index on payments.txid

--------------------------------------------------------------------

SYSTEM 8 — VERIFICATION HUB
  ✅ 7 forum threads configured
  ✅ Hub logging for critical actions
  ⚠️  "Real-time logging" — not confirmed which events are logged
  ❌ Admin hub commands not confirmed (/userinfo /warn /mute /ban
     /kick /approve /reject and all /broadcast_* variants)
  ❌ No confirmation admin role check on hub commands

ACTION NEEDED:
  Verify all 11 hub commands are implemented and gated by admin check.

--------------------------------------------------------------------

SYSTEM 9 — BROADCAST
  ✅ Text, photos, albums, files supported
  ✅ FloodWait protection
  ✅ Progress tracking
  ⚠️  FSM-based — confirm FSM state is not the only record
      (broadcast job should be DB-backed for restart safety)
  ❌ Video NOT confirmed
  ❌ Voice NOT confirmed
  ❌ Audio NOT confirmed
  ❌ GIF/animation NOT confirmed
  ❌ Sticker NOT confirmed
  ❌ Caption preservation on ALL types NOT confirmed
  ❌ Album (media_group) collection window (1-2 sec) NOT confirmed
  🔴 CRITICAL — "text, photos, albums, files" is only 4 types.
      Spec requires ALL content types admin may want to send.

ACTION NEEDED:
  Inspect broadcast handler. Must handle:
  text, photo, video, voice, audio, document, animation,
  sticker, media_group — each with correct API method.
  Caption must be preserved on every media type.

--------------------------------------------------------------------

SYSTEM 10 — SUPPORT TICKET
  ✅ Accept Support flow implemented
  ✅ Admins blocked from bridging until acceptance
  ✅ 5-minute inactivity monitor
  ✅ User stats on support card (Plan, Submissions, Bans)
  ⚠️  Support card — spec requires MORE fields:
      Name, Username, User ID, Join Date, Premium Status,
      Referral Stats, Submission Stats, Ban History, Mute History
      "Plan, Submissions, Bans" is incomplete
  ❌ Ticket closure flow not mentioned:
      - User-side messages deleted on closure
      - Topic preserved permanently
      - Closure summary saved to DB
  ❌ Auto-open support from unhandled user text not confirmed

ACTION NEEDED:
  Verify support card has ALL required fields.
  Verify closure handler deletes user-side messages silently.
  Verify unhandled text opens support ticket.

--------------------------------------------------------------------

SYSTEM 11 — TAKEDOWN
  ✅ /takedown FSM implemented
  ✅ Validates Content IDs
  ✅ Collects reason and link
  ⚠️  "Validates Content IDs" — spec does not require ID validation,
      only free-text reason + content link. Confirm this is not
      blocking users who submit URLs instead of IDs.
  ❌ Admin card format not confirmed (formatted box with all fields)
  ❌ Approve & Delete → auto-delete attempt not confirmed
  ❌ Manual deletion confirm button not confirmed
  ❌ Reject → admin must TYPE reason (mandatory) not confirmed
  ❌ Reject → auto-open support session not confirmed
  ❌ takedown_id, support_ticket_id linkage in DB not confirmed

ACTION NEEDED:
  Verify reject flow forces admin to type reason before completing.
  Verify rejection auto-opens support with rejection context.

--------------------------------------------------------------------

SYSTEM 12 — CONTENT SUBMISSION
  ❌ NOT MENTIONED in Gemini's report
  ⚠️  System 13 mentions "Reject with Reason → Auto-Support" which
      overlaps — but full submission system not confirmed

  Not confirmed:
    - Channel membership gate
    - Terms acceptance
    - All 8 content types supported
    - Duplicate hash detection
    - Moderation buttons (🔞 🔥 ⭐ ❌)
    - Vault + Queue on approval
    - User notified on rejection with reason

ACTION NEEDED:
  Verify submission handler and all 4 moderation button callbacks.

--------------------------------------------------------------------

SYSTEM 13 — VAULT & QUEUE ENGINE
  ⚠️  "Standardized distribution delays to 1-5 minutes" — mentioned
      under watermark section, not queue engine directly
  ❌ Queue states (PENDING → PROCESSING → SENT → FAILED → DEAD_LETTER)
     not confirmed
  ❌ Dead letter queue not confirmed
  ❌ Daily limits (PREMIUM_DAILY_LIMIT, NSFW_DAILY_LIMIT) not confirmed
  ❌ Vault random reposting / fair rotation not confirmed
  🔴 CRITICAL — Queue engine drives all content distribution

ACTION NEEDED:
  Verify queue_jobs table has status column with all states.
  Verify dead_letters table exists.
  Verify ENV variables for daily limits are read.

--------------------------------------------------------------------

SYSTEM 14 — WATERMARK PIPELINE
  ✅ FFmpeg specs fixed (BOTTOM_RIGHT, 15% scale, 0.8 opacity)
  ⚠️  Spec says: random position, NOT fixed BOTTOM_RIGHT
      Spec says: opacity 90 for photos (not 0.8 which is same thing,
      but video opacity should be 110-130 — confirm video watermark
      has TWO text layers at random position and random timing)
  ❌ Photo watermark: PNG overlay confirmed but random position
     contradicts Gemini's "BOTTOM_RIGHT" fix

ACTION NEEDED:
  Clarify with client: fixed position or random position?
  Spec says random. If client wants fixed, update spec.
  Verify video has TWO text watermark layers, not one.

--------------------------------------------------------------------

SYSTEM 15 — REFERRAL SYSTEM
  ❌ NOT MENTIONED in Gemini's report
  
  Not confirmed:
    - Personal referral link generated on registration
    - Referral counted only after join + membership verify
    - Duplicate prevention
    - Points: 1 referral = 1 point, 3 approvals = 1 point
    - ৳1 discount per point
    - Dashboard: total / active / blocked / points

ACTION NEEDED:
  Verify referrals table and referral_link column in users table.

--------------------------------------------------------------------

SYSTEM 16 — USER STATUS DASHBOARD
  ❌ NOT MENTIONED in Gemini's report

  Not confirmed: all 19 display fields from spec Section 17

ACTION NEEDED:
  Verify My Status handler returns all required fields.

--------------------------------------------------------------------

SYSTEM 17 — MESSAGE CLEANUP
  ✅ ./prefix → 10 seconds (System 4)
  ❌ 60-minute general cleanup NOT confirmed
  ❌ 20-minute payment session cleanup NOT confirmed
  ❌ 7-minute BD phone number cleanup NOT confirmed
     Pattern: 01[3-9]\d{8}
  ❌ Support closure cleanup NOT confirmed

ACTION NEEDED:
  Check for a cleanup worker / scheduled task that handles
  all 5 cleanup rules. This is likely completely missing.

--------------------------------------------------------------------

SYSTEM 18 — TRUST & FRAUD SYSTEM
  ⚠️  "Atomic reward point locking" mentioned under System 14
      (Gemini's numbering) — this maps to payment/rewards, not
      trust score system
  ❌ trust_score column in users table not confirmed
  ❌ fraud_score column in users table not confirmed
  ❌ Score update triggers not confirmed

ACTION NEEDED:
  Check DB schema for trust_score and fraud_score columns.

--------------------------------------------------------------------

SYSTEM 19 — AUDIT LOGGING
  ✅ Hub logging implemented for critical actions
  ⚠️  "Critical actions" is vague — need all events covered:
      Payments, Subscriptions, Support, Moderation, Invites,
      Broadcasts, Queue actions, Trust/Fraud changes, Takedowns
  ❌ audit_logs DB table not confirmed (only hub forwarding mentioned)

ACTION NEEDED:
  Verify audit_logs table exists in DB schema.
  Verify events are written to DB AND forwarded to hub topic.

--------------------------------------------------------------------

SYSTEM 20 — RESTART SAFETY
  ❌ NOT MENTIONED in Gemini's report
  🔴 CRITICAL — Core principle of entire platform

  Not confirmed:
    - On startup: active sessions restored from DB
    - Pending payments resumed
    - Active support tickets reloaded
    - Queue workers restarted from DB state

ACTION NEEDED:
  Check startup/on_startup hook in main.py or bot lifecycle.
  Must query DB for active sessions and restore them.

====================================================================
SUMMARY OF CRITICAL GAPS
====================================================================

🔴 CRITICAL — Must verify/fix before accepting delivery:

  1. System 5  — Payment system (entire flow) — NOT reported
  2. System 6  — Subscription lifecycle — NOT reported
  3. System 7  — TXID protection — NOT reported
  4. System 9  — Broadcast missing: video, voice, audio, GIF,
                 sticker, caption preservation on all types
  5. System 12 — Content submission — NOT reported
  6. System 13 — Vault/Queue/Dead letter — NOT reported
  7. System 17 — Message cleanup (4 of 5 rules) — NOT reported
  8. System 20 — Restart safety — NOT reported

⚠️  VERIFY BEFORE SIGN-OFF:

  9.  Onboarding check is DB-based not FSM-based
  10. /main command removed from command list
  11. Support card has ALL required fields (not just 3)
  12. Takedown reject forces typed reason + auto-opens support
  13. Watermark is random position (spec) vs BOTTOM_RIGHT (Gemini)
  14. Referral system exists
  15. User dashboard returns all 19 fields
  16. audit_logs written to DB (not just hub forwarding)

====================================================================
NEXT PROMPT TO SEND GEMINI
====================================================================

Paste this to Gemini now:

---

Your report shows 8 critical systems were either not implemented
or not reported. Before this delivery is accepted, you must address
each item below. Do not skip any.

CRITICAL FIXES NEEDED:

1. PAYMENT SYSTEM (System 5)
   Confirm or implement the full payment flow end-to-end:
   package selection → method → DB record → hub topic card →
   admin "Send Payment Details" button → session activates on
   delivery success only → 20-min timeout → TXID proof collection →
   admin approve/reject → atomic 9-step subscription activation →
   one-time invite (30min expiry) → delivery to user.

2. SUBSCRIPTION LIFECYCLE (System 6)
   Confirm or implement: expiry notification worker sending
   messages at 7 days, 3 days, and expiry day.
   Auto-remove user from premium group on expiry.
   Update DB status to EXPIRED.

3. TXID PROTECTION (System 7)
   Confirm or implement: txid_registry table with unique constraint.
   Duplicate TXID auto-rejected before admin sees proof.

4. BROADCAST — MISSING CONTENT TYPES (System 9)
   Current broadcast handles: text, photos, albums, files.
   Add support for: video, voice, audio, GIF/animation, sticker.
   Confirm caption is preserved on every content type.
   Confirm album (media_group) uses a 1-2 second collection
   window to gather all group messages before forwarding.

5. CONTENT SUBMISSION (System 12)
   Confirm or implement: channel membership gate, terms acceptance,
   all 8 content types, duplicate hash detection, moderation
   buttons (🔞 🔥 ⭐ ❌), vault + queue on approval, user
   notified on rejection with reason.

6. VAULT & QUEUE ENGINE (System 13)
   Confirm or implement: queue_jobs table with status column
   (PENDING/PROCESSING/SENT/FAILED/DEAD_LETTER), dead_letters table,
   daily limits from ENV (PREMIUM_DAILY_LIMIT, NSFW_DAILY_LIMIT),
   vault random reposting with fair rotation and cooldown.

7. MESSAGE CLEANUP (System 17)
   Implement all 5 cleanup rules:
   - General messages → auto-delete after 60 minutes
   - Payment session messages → auto-delete after 20 minutes
   - BD phone numbers (01[3-9]\d{8}) → auto-delete after 7 minutes
   - ./prefix → already done (10 seconds)
   - Support user-side messages → delete on ticket closure
   All deletions must be completely silent.

8. RESTART SAFETY (System 20)
   Implement on_startup hook that:
   - Queries DB for all active payment sessions and resumes timers
   - Queries DB for all open support tickets and reloads bridges
   - Restarts queue worker from last DB state
   - Logs restart recovery to audit

VERIFY AND REPORT BACK ON:
   - Onboarding check uses DB column, not FSM state
   - /main command does NOT appear in bot command list
   - Support card shows all fields: Name, Username, User ID,
     Join Date, Premium Status, Referral Stats, Submission Stats,
     Ban History, Mute History
   - Takedown reject: admin must TYPE reason (bot prompts and waits),
     then rejection auto-opens support session with rejection context
   - Referral system exists with referral_link in users table
   - My Status dashboard returns all 19 required fields
   - audit_logs table exists in DB (not just hub forwarding)

After completing all of the above, push again with commit message
describing exactly what was added, and provide the new commit hash.

---

====================================================================
END OF CROSS-CHECK REPORT
====================================================================