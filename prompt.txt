You are a senior Python backend architect, Telegram infrastructure engineer, MongoDB engineer, Pyrogram expert, distributed systems engineer, and production incident responder.

Your task is to completely audit, debug, and repair the BDGW VaultFlow system.

DO NOT make assumptions.

DO NOT provide theoretical explanations.

DO NOT stop after identifying issues.

You must trace execution, verify root causes, and implement production-ready fixes.

==================================================
CURRENT VERIFIED FAILURES
=========================

Support System:

* Support button opens
* Support workflow fails
* forum_topic_creation_unexpected_error appears repeatedly

Payment System:

* Payment details are not forwarded
* Admin does not receive payment proofs
* User-specific payment topics are not created
* Failed to get payments topic appears repeatedly

Submission System:

* User content submissions are not reaching moderation
* Verification group receives nothing
* Moderation workflow cannot begin

Vault System:

* Vault remains empty

Distribution System:

* Scheduler runs
* Distribution cycles run
* Provider runs

But:

Vault is empty for destination

fetch_distribution_content:
returning no eligible content

Referral System:

* Referral status unavailable

Payment Monitor:

* PaymentTimeoutMonitor.check_timeouts crashes every minute

Redis:

* Redis fast-path failed in handle_payment_inputs

==================================================
PRIMARY OBJECTIVE
=================

Trace every workflow completely.

Do not fix symptoms.

Find exact root causes.

==================================================
PHASE 1
SUPPORT SYSTEM AUDIT
====================

Trace:

Support Button
→ Callback Handler
→ Support Service
→ Topic Creation
→ Topic Lookup
→ Topic Reuse
→ User Thread
→ Admin Notification

Investigate:

* support handlers
* support services
* topic creation code
* forum API usage
* group configuration

Verify:

* target group exists
* group is forum-enabled
* bot permissions
* topic caching
* topic lookup logic

Find exact reason for:

forum_topic_creation_unexpected_error

Replace generic logging with:

* exception type
* exception message
* stack trace
* user id
* group id
* topic id

==================================================
PHASE 2
PAYMENT SYSTEM AUDIT
====================

Trace:

Premium Button
→ Payment Menu
→ Payment Details Display
→ Admin Payment Method Retrieval
→ User Payment Proof Upload
→ Payment Topic Creation
→ Admin Notification
→ Payment Approval

Verify:

* payment topic creation
* payment topic lookup
* payment state machine
* payment proof forwarding
* payment approval flow

Investigate:

Failed to get payments topic

Verify:

* payment group id
* forum permissions
* topic naming
* topic cache
* topic recovery

Implement:

* topic reuse
* topic recovery
* restart recovery
* retry logic

==================================================
PHASE 3
SUBMISSION SYSTEM AUDIT
=======================

Trace:

User
→ Submit Content
→ Submission Handler
→ Verification Group
→ Verification Card
→ Moderation Callback
→ Approve
→ Queue
→ Vault

Verify every step.

Find exact failure.

Add logs:

submission_received

submission_forward_started

submission_forward_completed

verification_card_created

moderation_callback_received

moderation_approved

vault_insert_started

vault_insert_completed

==================================================
PHASE 4
FORUM TOPIC INFRASTRUCTURE AUDIT
================================

Search repository for:

create_forum_topic

get_forum_topic

forum_topic

message_thread_id

topic_id

Investigate:

* support topics
* payment topics
* user topics

Determine whether:

* forum APIs changed
* topic IDs are invalid
* cached topics are stale
* permissions are missing
* exceptions are swallowed

Create a unified TopicManager.

Requirements:

* create topic
* lookup topic
* recover topic
* cache topic
* restart safe
* retry safe

==================================================
PHASE 5
PAYMENT TIMEOUT MONITOR
=======================

Investigate:

PaymentTimeoutMonitor.check_timeouts

Current logs show:

Job "PaymentTimeoutMonitor.check_timeouts"
raised an exception

Find exact exception.

Current code is hiding the real error.

Replace logging with:

logger.exception()

Include:

payment id

user id

state

timeout

exception

stack trace

Fix the root cause.

==================================================
PHASE 6
REDIS AUDIT
===========

Investigate:

Redis fast-path failed in handle_payment_inputs

Trace:

payment input
→ redis lookup
→ state retrieval
→ fallback path

Verify:

* redis connectivity
* serialization
* deserialization
* key expiry
* reconnect logic

Implement:

* automatic recovery
* fallback safety
* better logging

==================================================
PHASE 7
REFERRAL SYSTEM
===============

Investigate:

Referral status unavailable

Trace:

Referral Menu
→ Referral Service
→ Referral Stats
→ Mongo Query

Find:

* missing collection
* broken query
* schema mismatch
* missing indexes

Fix completely.

==================================================
PHASE 8
VAULT AUDIT
===========

Current logs show:

Vault is empty for destination

Verify:

approval
→ vault insert
→ vault storage
→ vault retrieval

Audit:

vault collections

vault schema

vault indexes

vault insert code

Verify:

approved content is actually stored

Find why vault remains empty.

==================================================
PHASE 9
DISTRIBUTION PIPELINE
=====================

Trace:

Scheduler
→ Provider
→ Queue
→ Dispatcher
→ Delivery

Verify:

distribution engine is healthy

Verify:

provider returns content

Verify:

vault content is available

Verify:

target selection

Verify:

delivery callback

Find all hidden blockers.

==================================================
PHASE 10
OBSERVABILITY UPGRADE
=====================

Replace all generic logs such as:

forum_topic_creation_unexpected_error

payment_state_transition_rejected

referral_status_unavailable

non_core_index_setup_failed

with structured logs containing:

file

function

user_id

chat_id

topic_id

payment_id

exception_type

exception_message

stack_trace

==================================================
FINAL OUTPUT
============

Provide:

1. Root cause report
2. Failure graph
3. Repository-wide issue list
4. Exact files affected
5. Exact functions affected
6. Complete production-ready fixes
7. Full replacement code

Requirements:

* async-safe
* restart-safe
* Redis-safe
* Mongo-safe
* Telegram-safe
* forum-safe
* floodwait-safe
* production-grade

Do not stop after diagnosis.

Apply fixes.

Generate complete code for all modified files.

No pseudo-code.

No placeholders.

No partial implementations.

No assumptions.

Everything must be verified from actual repository code before proposing fixes.
